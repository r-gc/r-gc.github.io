---
title: Frappe / ERPNext Docker 实践：从容器化环境到业务网关
published: 2026-05-20
description: 整理 myapp 在 Frappe / ERPNext Docker 环境中的后端网关、前端集成、移动端和测试体系。
tags: [Frappe, ERPNext, Docker, API, React Native]
category: 项目复盘
draft: false
---

这个项目不是简单地把 Frappe Docker 跑起来，而是在 Frappe / ERPNext 的容器化环境里，围绕一个自定义业务应用 `myapp` 做了比较完整的二次开发。

从当前代码和文档来看，项目可以分成三层：

- Frappe / ERPNext 容器化运行环境
- `apps/myapp` 自定义后端应用
- `frontend/myapp-web` 和 `frontend/myapp-mobile` 前端客户端

## 项目定位

`myapp` 是一个基于 Frappe / ERPNext 的业务系统扩展。它没有直接让前端散落调用 ERPNext 原生接口，而是用 `myapp.api.gateway.*` 组织出一层更稳定的业务网关。

这层网关覆盖了商品、客户、销售、采购、供应商、结算、报表、媒体上传和移动端版本信息等能力。

这样的好处是：

- 前端面对的是业务语义，而不是 ERPNext 的底层 DocType 细节
- 错误码、响应格式和权限处理可以统一
- 销售、采购等主链路可以围绕幂等和真实 HTTP 测试做回归
- 后续替换图片存储、打印服务或移动端能力时，接口契约更稳定

## 技术栈

后端主要基于：

- Frappe / ERPNext
- Python
- Frappe 自定义 App
- Docker / Devcontainer
- Postman 接口集合
- unittest HTTP / integration tests

前端部分包括：

- `myapp-web`：React 19、Umi Max、Ant Design Pro
- `myapp-mobile`：Expo、React Native、expo-router、相机、图片、打印、PDF 等能力

这个组合说明项目不是只停留在后台配置，而是有 Web 管理端和移动作业端的落地意图。

## 后端网关设计

文档里把接口按业务域组织，而不是按“原生接口 / 自定义接口”划分。这一点很重要，因为调用方真正关心的是“销售要调哪些接口”“采购要调哪些接口”“报表要调哪些接口”。

当前网关包含这些方向：

- 商品搜索、商品创建、商品详情、图片上传
- 客户与供应商主数据
- 销售订单、发货、开票、收款、退货
- 采购订单、收货、采购发票、供应商付款、采购退货
- 经营总览、销售分析、采购分析、应收应付、资金流水
- 移动端版本信息与认证

接口返回也统一成类似这样的包络：

```json
{
  "ok": true,
  "status": "success",
  "code": "ORDER_CREATED",
  "message": "业务提示信息",
  "data": {},
  "meta": {}
}
```

错误响应也做了统一错误码，例如认证、权限、资源不存在、库存不足、参数校验失败等。这比前端分别处理各种 Frappe 异常要清晰很多。

## 采购链路

采购模块的设计文档比较完整。它把采购拆成：

- 创建采购订单
- 按实际到货生成采购收货单
- 按实际收货生成采购发票
- 供应商付款登记
- 采购退货

这里有一个关键设计原则：采购订单表示“预期采购”，真正库存入账以 `Purchase Receipt` 为准。也就是说，如果供应商实际到货数量和订单数量不一致，系统应该允许按实收数量收货和结算。

这比“采购订单一建就假设全部到货”的实现更接近真实业务。

## 移动端与现场作业

移动端依赖里包含相机、图片选择、图片压缩、打印、分享、PDF、图表等能力，这和文档里的业务方向是对得上的：

- 到货收货适合移动端现场作业
- 商品图片上传需要移动端处理
- 采购、销售、打印预览适合平板或手机完成
- 报表和状态聚合适合移动端快速查看

这也是这个项目的一个亮点：它不是单纯的 ERP 后台页面，而是试图把 ERPNext 的底层能力包装成更适合移动业务场景的应用。

## 测试体系

项目文档强调优先使用 HTTP 测试，而不是在宿主机里直接导入 Frappe 服务层。

原因是 HTTP 测试更接近真实调用路径，可以覆盖：

- 鉴权
- 权限
- 路由
- 响应包装
- 真实主数据
- devcontainer 网络环境

文档中记录了多组测试：

- 销售与采购主链路
- 商品工作台与销售状态聚合
- 采购快捷链路
- 幂等、并发和不同数据重放
- 单位换算与库存结算回归
- 性能基线脚本

这让项目更像一个可持续演进的业务系统，而不是一次性的页面 demo。

## 可以继续完善的方向

后续可以把这篇项目复盘继续扩展成几篇更细的文章：

- Frappe / ERPNext 自定义业务网关设计
- 采购链路如何处理“订单数量”和“实际到货”的差异
- 移动端如何对接 ERPNext 业务接口
- 商品图片上传、临时文件绑定和清理策略
- HTTP 测试如何覆盖 ERPNext 自定义接口

目前最值得优先补的，是截图和一条完整业务链路的可视化流程图。这样项目展示会更直观。
